services:
  postgres:
    image: postgres:16-alpine
    container_name: maxima_postgres
    environment:
      POSTGRES_USER: dagster
      POSTGRES_PASSWORD: dagster
      POSTGRES_DB: dagster
    volumes:
      - dagster_postgres_data:/var/lib/postgresql/data
    healthcheck:
      test: ["CMD-SHELL", "pg_isready -U dagster -d dagster"]
      interval: 10s
      timeout: 5s
      retries: 5

  dagster-webserver:
    build:
      context: .
      dockerfile: Dockerfile
    container_name: maxima_dagster_webserver
    command: ["dagster-webserver", "-h", "0.0.0.0", "-p", "3000", "-w", "/opt/dagster/app/workspace.yaml"]
    environment:
      DAGSTER_HOME: /opt/dagster/dagster_home
      GIRDER_API_URL: ${GIRDER_API_URL}
      GIRDER_API_KEY: ${GIRDER_API_KEY}
      GIRDER_ROOT_FOLDER_ID: ${GIRDER_ROOT_FOLDER_ID}
      GIRDER_CALIBRANTS_FOLDER_ID: ${GIRDER_CALIBRANTS_FOLDER_ID}
      GIRDER_MODEL_ITEM_ID: ${GIRDER_MODEL_ITEM_ID}
    ports:
      - "3000:3000"
    volumes:
      - ./dagster_home:/opt/dagster/dagster_home
      - ./data:/opt/dagster/app/data
    depends_on:
      postgres:
        condition: service_healthy

  dagster-daemon:
    build:
      context: .
      dockerfile: Dockerfile
    container_name: maxima_dagster_daemon
    command: ["dagster-daemon", "run", "--code-server-log-level", "error", "-w", "/opt/dagster/app/workspace.yaml"]
    environment:
      DAGSTER_HOME: /opt/dagster/dagster_home
      GIRDER_API_URL: ${GIRDER_API_URL}
      GIRDER_API_KEY: ${GIRDER_API_KEY}
      GIRDER_ROOT_FOLDER_ID: ${GIRDER_ROOT_FOLDER_ID}
      GIRDER_CALIBRANTS_FOLDER_ID: ${GIRDER_CALIBRANTS_FOLDER_ID}
      GIRDER_MODEL_ITEM_OR_FILE_ID: ${GIRDER_MODEL_ITEM_OR_FILE_ID}
    volumes:
      - ./dagster_home:/opt/dagster/dagster_home
      - ./data:/opt/dagster/app/data
    depends_on:
      postgres:
        condition: service_healthy

volumes:
  dagster_postgres_data:
